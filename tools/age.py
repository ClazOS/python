# Age calculator
from datetime import datetime
def countdown(birth_month,birth_day,unit):
    # birth_month = integer 0 (January) through 11 (December)
    # birth_day = integer
    # unit = "days", "hours", or "minutes"
    "REPLACE THIS CODE WITH YOUR countdown METHOD"
    now = datetime.today()

def timehere(birth_month,birth_day,birth_year,unit):
    # birth_month = integer 0 (January) through 11 (December)
    # birth_day = integer
    # birth_year = integer
    # unit = "days", "hours", or "minutes"
    "REPLACE THIS CODE WITH YOUR timehere METHOD"
    
    
